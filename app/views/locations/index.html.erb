<section id="location-type">
 <header id="location-type-title">
  <h1>
   <%= @location_type.title %>
  </h1>
 </header>
 <ul> 
  <% @locations.each do |loc| %>
   <li><%= link_to loc.title, location_path(loc) %></li>
  <% end %>
 </ul>
</section>
