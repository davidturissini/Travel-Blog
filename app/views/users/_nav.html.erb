<nav id="user-nav">
	<ul>
		<li class="trips">
			<%= link_to "Travels", user_trips_path(:user_id => user.slug) %>
		</li>
		<li class="maps">
			<%= link_to "Map", user_maps_path(:user_id => user.slug) %>
		</li>
	</ul>
	<% if !current_user.anonymous? %>
		<ul class="admin">
			<li>
				<%= link_to "Add new trip", new_trip_path %>
			</li>
			<li>
				<%= link_to "Edit trips", "/" %>
			</li>
		</ul>
	<% end %>
</nav>