<%= render "admin/global/breadcrumb",
	:trip => @trip,
	:crumbs => [
		{ :title => "Photos", :url => user_trip_photos_path(:user_id => @trip.user.slug, :trip_id => @trip.slug) }
		],
	:page => "New"
%>
<section id="trip" class="pad" data-json="<%= @trip.to_json %>">
	<%= render "admin/global/photo_uploader", :obj => @trip %>
</section>